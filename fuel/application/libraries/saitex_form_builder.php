<?php

require_once(FUEL_PATH . '/libraries/Form_builder.php');

Class Saitex_form_builder extends Form_builder {
 

    public function render_divs($fields = NULL) {
        return parent::render_divs($fields);
    }
 

    // --------------------------------------------------------------------
}
